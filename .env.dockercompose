# Postgres configuration
POSTGRES_DB=palmyra
POSTGRES_USER=palmyra
POSTGRES_PASSWORD=palmyra

# API configuration
PORT=3000
REQUEST_TIMEOUT=15s
SHUTDOWN_TIMEOUT=10s
LOG_LEVEL=info
DATABASE_URL=postgres://palmyra:palmyra@postgres:5432/palmyra?sslmode=disable
ENV_KEY=dev
ADMIN_TENANT_SLUG=admin
ADMIN_TENANT_NAME=Admin
AUTH_PROVIDER=dev
# Storage configuration
STORAGE_BACKEND=local
# When STORAGE_BACKEND=gcs, set STORAGE_BUCKET to your bucket name, e.g.:
# STORAGE_BUCKET=palmyra-dev-bucket
# When STORAGE_BACKEND=local, set the base directory for local storage (defaults below):
STORAGE_LOCAL_DIR=./.data/storage
# Uncomment to point to Firebase credentials inside the container
# FIREBASE_CONFIG=/app/firebase/service-account.json
# GCLOUD_PROJECT=your-project-id
