API app (Go/Chi)

This directory will contain the API entrypoint for the backend (Chi router), global middleware wiring, and per‑domain route mounting. No domain logic lives here; it only composes handlers from domains/*/be.
