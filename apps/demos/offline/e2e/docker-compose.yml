services:
  playwright:
    build:
      context: ../../../..
      dockerfile: apps/demos/offline/e2e/playwright.Dockerfile
    working_dir: /workspace
    environment:
      CI: "true"
    volumes:
      - ../../../..:/workspace
