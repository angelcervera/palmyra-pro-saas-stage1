# Scope mapping for company packages
# If you use GitHub Packages, keep this value. Otherwise replace with your private registry URL.
@tcglanddev:registry=https://npm.pkg.github.com

# Optional: uncomment if you want to set the default registry explicitly
# registry=https://registry.npmjs.org/

# Always save exact versions on install (pnpm prefers lockfile; this is a fallback)
save-exact=true

