/**
 * Entry point placeholder for @zengateglobal/persistence-sdk.
 *
 * The final implementation will export the shared PersistenceClient interface
 * plus factory helpers for online/offline adapters. For now we keep the file
 * so the module structure is ready.
 */
export {};
