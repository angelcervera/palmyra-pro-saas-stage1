// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type { TenantsCreateData, TenantsCreateErrors, TenantsCreateResponses, TenantsGetData, TenantsGetErrors, TenantsGetResponses, TenantsListData, TenantsListErrors, TenantsListResponses, TenantsProvisionData, TenantsProvisionErrors, TenantsProvisionResponses, TenantsProvisionStatusData, TenantsProvisionStatusErrors, TenantsProvisionStatusResponses, TenantsUpdateData, TenantsUpdateErrors, TenantsUpdateResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean> = Options2<TData, ThrowOnError> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * List tenants (admin only)
 */
export const tenantsList = <ThrowOnError extends boolean = false>(options?: Options<TenantsListData, ThrowOnError>) => {
    return (options?.client ?? client).get<TenantsListResponses, TenantsListErrors, ThrowOnError>({
        url: '/admin/tenants',
        ...options
    });
};

/**
 * Provision a new tenant (admin only)
 *
 * Creates a tenant record and returns derived routing fields. Derived values are computed server-side: schemaName uses `tenant_<slugSnake>`, basePrefix uses `<envKey>/<tenantSlug>-<shortTenantId>/`, and shortTenantId is the first 8 hex characters of tenantId.
 */
export const tenantsCreate = <ThrowOnError extends boolean = false>(options: Options<TenantsCreateData, ThrowOnError>) => {
    return (options.client ?? client).post<TenantsCreateResponses, TenantsCreateErrors, ThrowOnError>({
        url: '/admin/tenants',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Get tenant by id (admin only)
 */
export const tenantsGet = <ThrowOnError extends boolean = false>(options: Options<TenantsGetData, ThrowOnError>) => {
    return (options.client ?? client).get<TenantsGetResponses, TenantsGetErrors, ThrowOnError>({
        url: '/admin/tenants/{tenantId}',
        ...options
    });
};

/**
 * Update tenant display or status (admin only)
 */
export const tenantsUpdate = <ThrowOnError extends boolean = false>(options: Options<TenantsUpdateData, ThrowOnError>) => {
    return (options.client ?? client).patch<TenantsUpdateResponses, TenantsUpdateErrors, ThrowOnError>({
        url: '/admin/tenants/{tenantId}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Provision or reprovision tenant environment (admin only)
 *
 * Creates or retries provisioning of tenant environment resources in one step: PostgreSQL schema and base tables, external auth tenant, and any other required infrastructure. Returns the updated provisioning state.
 */
export const tenantsProvision = <ThrowOnError extends boolean = false>(options: Options<TenantsProvisionData, ThrowOnError>) => {
    return (options.client ?? client).post<TenantsProvisionResponses, TenantsProvisionErrors, ThrowOnError>({
        url: '/admin/tenants/{tenantId}:provision',
        ...options
    });
};

/**
 * Check provisioning status (admin only)
 *
 * Performs a live check against backing systems to verify whether tenant provisioning has succeeded (DB schema exists, auth tenant exists, etc.). Updates stored provisioning state if a change is detected, then returns the current status.
 */
export const tenantsProvisionStatus = <ThrowOnError extends boolean = false>(options: Options<TenantsProvisionStatusData, ThrowOnError>) => {
    return (options.client ?? client).get<TenantsProvisionStatusResponses, TenantsProvisionStatusErrors, ThrowOnError>({
        url: '/admin/tenants/{tenantId}:provision-status',
        ...options
    });
};
